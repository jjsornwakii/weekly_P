#include <stdio.h>
#include <string.h>

int n,i,j,len,c[100000];
char t[1000000];
int main(){
    scanf("%s",t);
    len = strlen(t);

    for(i=0;i<len;i++){

        do{
            if(t[i]>=48&&t[i]<=57){
            
            }
        }while(t[i]>=48&&t[i]<=57);
    }
}